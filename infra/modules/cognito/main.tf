# Module: cognito
# Provisions a Cognito User Pool and App Client per environment.
# Each environment gets its own isolated User Pool (dev/prod never shared).
# Uses Hosted UI for MVP; custom UI can be layered on later.
# TODO: implement in Phase 1/3
